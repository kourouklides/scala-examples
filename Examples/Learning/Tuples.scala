object Tuples
{
    def main(args: Array[String])
    {
        def average(a: Double, b: Double): Double =
        {
            (a + b) / 2.0
        }

        println(average(10.0, 20.0))

    }
}
